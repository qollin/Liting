#include <ArduinoPinBank.h>
#include "Arduino.h"

const int ArduinoPinBank::digitalRead(uint8_t pin) {
	return digitalRead(pin);
}

const void ArduinoPinBank::digitalWrite(uint8_t pin, uint8_t value) {
	digitalWrite(pin, value);
}
